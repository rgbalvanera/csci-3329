#a = [int(i) for i in input().split()]
#print(a)
'''
class Robot:
    def __init__(self,name, battery, unit):
        self.name = name
        self.battery = battery
        self.unit = unit

    def displayName(self):
        print(self.name)

unit1 = Robot("Ramattra", 100, "R-700")
unit1.displayName()

#built in functions

hasattr(unit1, "attribute_name") #returns true if attribute exists

getattr(unit1, "attribute_name") #returns value of attribute

setattr(unit1, "attribute_name") # Set the attribute to a new value

delattr(unit1, "attribute_name") # Delete the attribute
'''

''' Printing true or false statements
print(1-1==0)
print(1-1!=0)'''

print("meow i love you")